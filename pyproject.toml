[tool.poetry]
name = "rdf"
version = "0.1.0"
description = ""
authors = ["Yiming Li", "Saray Bakker"]
readme = "readme.md"

[tool.poetry.dependencies]
python = "^3.10"
matplotlib = "^3.10.0"
torch = "^2.7.1"
pyyaml = "^6.0.2"
trimesh = "^4.6.12"
pyglet = "1.5.0"
argparse = "^1.4.0"
mesh-to-sdf = {git = "https://github.com/marian42/mesh_to_sdf.git"}

[tool.poetry.group.dev]
optional = true

[tool.poetry.group.dev.dependencies]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
